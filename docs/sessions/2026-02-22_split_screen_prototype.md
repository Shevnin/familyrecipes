# Сессия 2026-02-22 — Split Screen прототип

## Что сделали

1. **Заменили старый прототип** — удалили Core Flow v1 (`recipe.html`, `my.html`, `lib.js`, `styles.css`, `data/recipes.js`), создали `src/index.html` — split screen "Мама → Сын"

2. **Сделали телефонный UI** — два iPhone-подобных экрана рядом (нотч, скруглённые углы, тени) вместо плоских панелей

3. **Добавили вкладку "Отправленные"** у мамы — список отправленных рецептов с названием и превью

4. **Настроили деплой на GitHub Pages** — прототип доступен по URL:
   https://shevnin.github.io/familyrecipes/src/index.html

5. **Настроили мультиаккаунт в gh CLI** — добавили личный `Shevnin` рядом с рабочим `alexshevnin-cas`, поставили `direnv` для автопереключения по папке проекта

## Текущее состояние прототипа

- Левый экран (Мама / Передатчик): ввод названия + текст рецепта, кнопка "Отправить сыну", вкладка "Отправленные"
- Правый экран (Сын / Мастер): оригинал от мамы + захардкоженная структурированная версия (ингредиенты + шаги)
- Данные в памяти (JS-переменные), без localStorage/сервера
- Структурированная версия — мок (не парсинг)
